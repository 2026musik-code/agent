{
  "name": "agent-007",
  "version": "1.0.0",
  "description": "Luxurious AI Chat Agent 007 deployed on Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "private": true
}
